<link rel="stylesheet" href="/css/matths-theme.css" />
<link rel="stylesheet" href="/css/contrast.css" />
<%
  const sharedPublicUser =
    typeof user !== "undefined" ? user : null;
  const sharedPublicActivePage =
    typeof activePage === "string"
      ? activePage
      : "";
%>
<%- include("home-public-navigation", {
  activePage: sharedPublicActivePage,
  user: sharedPublicUser,
  suppressGlobalSkipLink: true,
}) %>
